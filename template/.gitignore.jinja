# === Python ===
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# === Project Setup ===
.env

# === Jupyter ===
.ipynb_checkpoints/

# === Typing & Formatting ===
.mypy_cache/
.ruff_cache/
.pytest_cache/

{% if not workspace_location_external %}# === Workspace (generated — gitignored, fully regenerable) ===
_workspace/
{% endif %}

# === Typst ===
publication/*.pdf
*.pdf

# === OS & IDE ===
.vscode/
.idea/
*.code-workspace
.DS_Store
Thumbs.db

# === Data ===
data/raw/*
!data/raw/.gitkeep
data/test/*
!data/test/.gitkeep

# Scrap, drafts, handovers — never committed (canon: documentation discipline)
.tmp/

# What the editor writes for a session it is driving: screenshots and plan
# documents. Same class as .tmp/ above — a plan is what somebody intended on one
# afternoon, and a stale one in the tree reads like a description of the project
.nimbalyst/
nimbalyst-local/

# === Claude Code (local overrides) ===
.claude/settings.local.json
