% main.tex — {{ project_name }}
% Engine: pdfLaTeX | Build: latexmk → build/

\documentclass[12pt, a4paper, oneside, listof=totoc]{scrreprt}

% --- Seitenlayout ---
\usepackage[left=3cm, right=2.5cm, top=2.5cm, bottom=2cm, includefoot]{geometry}

% --- Schrift & Typografie ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{microtype}

% --- Sprache ---
\usepackage[ngerman]{babel}
\usepackage{csquotes}

% --- Metadaten ---
\input{config.tex}

% --- Grundlegende Formatierung ---
\usepackage{setspace}
\onehalfspacing
\KOMAoptions{parskip=false}
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}

% --- Mathematik/Einheiten ---
\usepackage{siunitx}
\sisetup{locale=DE, per-mode=symbol, detect-all=true}

% --- Abbildungen ---
\usepackage{graphicx}
{% raw %}\graphicspath{{assets/}}{% endraw %}
\usepackage{xcolor}

% --- Tabellen ---
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}

% --- Unterabbildungen ---
\usepackage{subcaption}

% --- Literatur ---
\usepackage[backend=biber, style=authoryear-icomp, sorting=nyt,
  giveninits=true, maxcitenames=2, maxbibnames=99]{biblatex}
\addbibresource{\myBibPath}

% --- Hyperlinks ---
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=blue,
  pdftitle={\myTitle},
  pdfauthor={\myAuthor},
}
\usepackage{bookmark}

% --- Querverweise ---
\usepackage[ngerman]{cleveref}

% --- Witwen & Waisen ---
\usepackage[all]{nowidow}
\clubpenalty=10000
\widowpenalty=10000
\displaywidowpenalty=10000

% ======================================================================
\begin{document}

\hypersetup{pageanchor=false}
\input{cover/cover.tex}
\cleardoublepage
\hypersetup{pageanchor=true}

% \pagenumbering{arabic}
% \tableofcontents
% \cleardoublepage

% \listoffigures
% \cleardoublepage

% \listoftables
% \cleardoublepage

% === Hauptteil ===
% \input{chapters/01_einleitung/einleitung.tex}

% === Literatur ===
% \printbibliography[heading=bibintoc, title={Literaturverzeichnis}]

\end{document}
